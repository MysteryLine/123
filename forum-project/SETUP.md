# 论坛网站 - 开发环境设置指南

## 📋 前置要求

在开始开发之前，请确保您的系统上已安装以下软件：

### 1. Node.js 和 npm
- **下载**: [https://nodejs.org/](https://nodejs.org/)
- **推荐版本**: Node.js 18.x LTS 或更高版本
- **验证安装**:我下在这里E:\Program Files\nodejs\
  ```bash
  node --version
  npm --version
  ```

### 2. MongoDB
- **选项 A - 本地安装**: [https://www.mongodb.com/try/download/community](https://www.mongodb.com/try/download/community)
- **选项 B - MongoDB Atlas (云)**: [https://www.mongodb.com/cloud/atlas](https://www.mongodb.com/cloud/atlas)
- **验证连接** (本地): MongoDB 应该在 `mongodb://localhost:27017` 运行

### 3. Git (可选)
- **下载**: [https://git-scm.com/](https://git-scm.com/)

---

## 🚀 快速开始

### 第一步: 克隆或下载项目
```bash
# 如果你已经有了项目文件夹，跳过这一步
```

### 第二步: 安装后端依赖
```bash
cd backend
npm install
```

### 第三步: 配置后端环境变量
编辑 `backend/.env.local` 文件：
```env
MONGODB_URI=mongodb://localhost:27017/forum_db
JWT_SECRET=your-secret-key-12345  # 更改为安全的密钥
NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:3000
```

### 第四步: 启动后端服务器
```bash
cd backend
npm run dev
```
✅ 后端应该在 http://localhost:5000 运行

### 第五步: 安装前端依赖（新终端）
```bash
cd frontend
npm install
```

### 第六步: 配置前端环境变量
编辑 `frontend/.env.local` 文件：
```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
```

### 第七步: 启动前端开发服务器
```bash
cd frontend
npm run dev
```
✅ 前端应该在 http://localhost:3000 运行

---

## 🌐 访问应用

打开浏览器访问: **http://localhost:3000**

### 测试流程
1. 注册新账户
2. 登录到系统
3. 创建一个帖子
4. 添加评论
5. 实时聊天功能

---

## 📱 终端命令参考

### 后端命令
```bash
cd backend

# 开发模式（支持热重载）
npm run dev

# 生产模式
npm start

# 运行测试
npm test
```

### 前端命令
```bash
cd frontend

# 开发模式
npm run dev

# 生产构建
npm run build

# 启动生产服务器
npm start

# 运行测试
npm test

# ESLint 检查
npm run lint
```

---

## 🔧 故障排除

### 问题 1: MongoDB 连接错误
**症状**: `Error: connect ECONNREFUSED 127.0.0.1:27017`

**解决方案**:
1. 确保 MongoDB 已启动
2. 检查 `.env.local` 中的 MONGODB_URI 是否正确
3. 如果使用 MongoDB Atlas，更新连接字符串为云数据库 URI

### 问题 2: 端口已被占用
**症状**: `Error: listen EADDRINUSE: address already in use :::5000`

**解决方案**:
```bash
# Windows - 找到使用端口 5000 的进程
netstat -ano | findstr :5000

# 杀死进程 (替换 PID)
taskkill /PID <PID> /F

# 或改变后端端口 (.env.local)
PORT=5001
```

### 问题 3: npm 依赖冲突
**症状**: `npm ERR! peer dependencies conflict`

**解决方案**:
```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 问题 4: JWT 认证失败
**症状**: `Token 无效或已过期`

**解决方案**:
1. 确保前端和后端使用相同的 JWT_SECRET
2. 检查浏览器 localStorage 中的 token 是否存在
3. 在浏览器开发者工具中检查 Authorization header

---

## 📊 项目架构概览

```
┌─────────────────────────────────────────────┐
│         前端 (Next.js)                       │
│      http://localhost:3000                   │
└────────────┬────────────────────────────────┘
             │
             │ HTTP/WebSocket
             │
┌────────────▼────────────────────────────────┐
│      后端 (Express.js)                       │
│     http://localhost:5000                    │
│  ┌──────────────────────────────────────┐   │
│  │      API Routes                      │   │
│  │ ├─ /api/auth (认证)                 │   │
│  │ ├─ /api/posts (帖子)                │   │
│  │ └─ /api/posts/:id/comments (评论)  │   │
│  └──────────────────────────────────────┘   │
│  ┌──────────────────────────────────────┐   │
│  │      Socket.io                       │   │
│  │ (实时聊天功能)                      │   │
│  └──────────────────────────────────────┘   │
└────────────┬────────────────────────────────┘
             │
             │ Mongoose
             │
┌────────────▼────────────────────────────────┐
│      MongoDB 数据库                          │
│   mongodb://localhost:27017/forum_db         │
│  ┌──────────────────────────────────────┐   │
│  │ Collections:                         │   │
│  │ ├─ Users (用户)                    │   │
│  │ ├─ Posts (帖子)                    │   │
│  │ └─ Comments (评论)                │   │
│  └──────────────────────────────────────┘   │
└──────────────────────────────────────────────┘
```

---

## 🎓 学习资源

- **Next.js 文档**: [https://nextjs.org/docs](https://nextjs.org/docs)
- **Express.js 文档**: [https://expressjs.com/](https://expressjs.com/)
- **MongoDB 文档**: [https://docs.mongodb.com/](https://docs.mongodb.com/)
- **Mongoose 文档**: [https://mongoosejs.com/](https://mongoosejs.com/)
- **Socket.io 文档**: [https://socket.io/docs/](https://socket.io/docs/)

---

## 💡 开发建议

1. **使用 VS Code**: 推荐安装扩展
   - ESLint
   - Prettier
   - MongoDB for VS Code
   - REST Client

2. **代码质量**:
   - 遵循项目的命名规范
   - 定期运行 `npm test` 
   - 使用 `npm run lint` 检查代码

3. **Git 工作流**:
   ```bash
   # 创建新分支
   git checkout -b feature/新功能名称
   
   # 提交更改
   git add .
   git commit -m "描述你的更改"
   
   # 推送到远程
   git push origin feature/新功能名称
   ```

---

**更新日期**: 2025-12-03  
**维护者**: AI 开发团队
