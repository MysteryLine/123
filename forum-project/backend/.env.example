# MongoDB Atlas 连接字符串
MONGODB_URI=mongodb+srv://用户名:密码@集群地址/forum?retryWrites=true&w=majority

# JWT 密钥（自定义，保持与本地一致）
JWT_SECRET=your-jwt-secret-here

# 端口（Render 会自动分配，本地开发用 5000）
PORT=5000

# 前端地址（用于 CORS 配置）
CLIENT_URL=http://localhost:3000
